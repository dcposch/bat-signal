{
  "name": "bat-signal",
  "version": "0.0.0",
  "description": "summon your heros. save the city.",
  "main": "index.js",
  "author": "Bat Signal",
  "license": "ISC",
  "repository": "https://github.com/dcposch/bat-signal",
  "dependencies": {
  },
  "devDependencies": {
    "browserify": "^13.3.0",
    "node-static": "^0.7.9",
    "standard": "^8.6.0",
    "uglify-js": "^2.7.5",
    "watchify": "^3.8.0"
  },
  "scripts": {
    "postinstall": "npm run build",
    "start": "npm run test && npm run watch & (cd static && static -z --cache false .)",
    "build": "rm -rf static/bundle.js && npm run browserify",
    "watch": "watchify src/index.js -o static/bundle.js",
    "browserify": "NODE_ENV=production browserify src/index.js | uglifyjs -c -m > static/bundle.js",
    "test": "standard"
  }
}
