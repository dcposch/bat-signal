{
  "name": "bat-signal",
  "version": "0.0.0",
  "description": "summon your heros. save the city.",
  "main": "index.js",
  "author": "Bat Signal",
  "license": "ISC",
  "repository": "https://github.com/dcposch/bat-signal",
  "dependencies": {
    "body-parser": "^1.16.0",
    "brfs": "^1.4.3",
    "express": "^4.14.1",
    "serve-static": "^1.11.2",
    "urlsafe-base64": "^1.0.0",
    "web-push": "^3.2.1"
  },
  "devDependencies": {
    "browserify": "^13.3.0",
    "standard": "^8.6.0",
    "uglify-js": "^2.7.5",
    "watchify": "^3.8.0"
  },
  "scripts": {
    "postinstall": "npm run build",
    "start": "node src/server.js",
    "build": "rm -rf static/bundle.js && npm run browserify",
    "watch": "npm run test && watchify -t brfs src/index.js -o static/bundle.js",
    "browserify": "NODE_ENV=production browserify -t brfs src/index.js | uglifyjs -c -m > static/bundle.js",
    "test": "standard"
  }
}
